spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/the-mustakim/E-Commerce-Application-Microservices.git
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
server:
  port: 8888