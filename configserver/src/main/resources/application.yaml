spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/the-mustakim/E-Commerce-Application-Microservices.git
          search-paths: app-configuration

server:
  port: 8888